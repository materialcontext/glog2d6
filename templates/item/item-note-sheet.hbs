<header class="p-6 section mb-8">
    <input name="name" type="text" value="{{item.name}}" placeholder="Note Title" class="input text-bold w-full" />
</header>

<section class="sheet-body">
    <div class="form-group flex flex-col flex-gap-2 p-10">
        <textarea name="system.description" rows="16" class="input w-full">{{system.description}}</textarea>
    </div>
</section>
