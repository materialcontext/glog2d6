{{!-- Notes Tab --}}
<div class="tab" data-group="primary" data-tab="notes">
    <div class="notes-tab-content section p-10 mt-10">
        <label class="text-small text-bold text-upper text-primary mb-4 flex">
            <i class="fas fa-scroll mr-4"></i> Notes & Description
        </label>
        <textarea
            name="system.details.description"
            rows="16"
            class="input w-full"
            placeholder="Character background, notes, personality...">{{system.details.description}}</textarea>
    </div>
</div>
