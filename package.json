{
    "scripts": {
        "test": "vitest",
        "test:run": "vitest run",
        "test:ui": "vitest --ui",
        "test:coverage": "vitest run --coverage",
        "test:watch": "vitest --watch",
        "test:reporter": "vitest --reporter=html"
    },
    "dependencies": {
        "eslint": "^9.27.0"
    },
    "devDependencies": {
        "@vitest/ui": "^3.1.4",
        "jsdom": "^26.1.0",
        "vitest": "^3.1.4"
    }
}
